{
  "name": "wallet-tracker",
  "version": "0.0.1",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^14.11.2",
    "gts": "^3.1.1",
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1",
    "typescript": "~4.7.0"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint",
    "watch": "tsc -w",
    "dev": "ts-node ./src/index.js"
  },
  "dependencies": {
    "xrpl": "^2.6.0"
  }
}
